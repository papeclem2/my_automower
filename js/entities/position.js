/**
 * Position class | describe attributes, getters & setters of the automower position -> x, y and orientation
 * @author clem
 */
export default class Position {

    constructor(x, y, orientation) {

        this.x = x;
        this.y = y;
        this.orientation = orientation;
    }
}